<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="tier_definition_view_form" model="ir.ui.view">
        <field name="name">tier.definition.form</field>
        <field name="model">tier.definition</field>
        <field name="inherit_id" ref="base_tier_validation.tier_definition_view_form"/>
        <field name="arch" type="xml">       
            <field name="definition_domain" position="after">
                <field name="additional_conditions"/>
                <field name="amount" invisible="additional_conditions != 'total_amount_spent_over_period_of_time'"/>
                <label for="period" invisible="additional_conditions != 'total_amount_spent_over_period_of_time'"/>
                <div class="o_row" invisible="additional_conditions != 'total_amount_spent_over_period_of_time'">
                    <field name="period" class="o_hr_narrow_field" nolabel="1"/>
                    <span> days</span>
                </div>
            </field> 
        </field>
    </record>
</odoo>
